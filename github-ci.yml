on:
  push:
    branches: [ main ]

jobs:
  include:
    
    -language: java
     jdk: jdk-8
     cache:
      directories:
        - $HOME/.m2
        
     before_install: cd Spring
     install: true
     
     script: mvn -B test
     
     deploy:
      skip_cleanup: true
      provider: heroku
      api_key:
        secure:
      app: shopping-list-fswp
      on: main